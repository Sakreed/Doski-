По купи-продай собираю стату скриптом: // Находим все элементы <span>, у которых атрибут title равен "Количество просмотров объявления"
var elements = document.querySelectorAll('span[title="Количество просмотров объявления"]');

// Инициализируем переменную для хранения суммы просмотров
var totalViews = 0;

// Проходим по найденным элементам и добавляем их значение к сумме
elements.forEach(function(element) {
    // Получаем текст внутри элемента и преобразуем его в число
    var views = parseInt(element.textContent.trim(), 10);

    // Если удалось преобразовать текст в число, добавляем его к общей сумме
    if (!isNaN(views)) {
        totalViews += views;
    }
});

// Выводим общую сумму просмотров в консоль
console.log('Общее количество просмотров на странице: ' + totalViews);
